<nav class="navbar sticky-top navbar-expand-lg navbar-dark" style="background-color: #1e3a8a;">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>

        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link <%= currentPath === '/' ? 'fw-bold' : '' %>"href="/">Home</a>
                <a class="nav-link <%= currentPath.startsWith('/cards') ? 'fw-bold' : '' %>"href="/cards">Your Collection</a>
            </div>
        </div>
    </div>
</nav>